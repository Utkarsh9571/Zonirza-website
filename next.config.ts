import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'upload.wikimedia.org', 'i.pravatar.cc', 'api.qrserver.com'],
  },
};

export default nextConfig;
